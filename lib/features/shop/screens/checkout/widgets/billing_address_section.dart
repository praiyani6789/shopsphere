import 'package:flutter/material.dart';

class SBillingAddreeSection extends StatelessWidget {
  const SBillingAddreeSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}